import { describe, beforeAll, beforeEach, afterEach, afterAll, it, expect } from 'hypium/index'

export default function indexTest() {
  describe('indexTestTest', function () {
    it('assertEqual', 0, function () {

    })
  })
}